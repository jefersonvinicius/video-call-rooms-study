import { BrowserRouter } from 'react-router-dom';
import RoutesDefinition from 'routes';

function App() {
  return (
    <BrowserRouter>
      <RoutesDefinition />
    </BrowserRouter>
  );
}

export default App;
